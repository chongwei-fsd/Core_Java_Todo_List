package mini_proj;

public class FloatingTask extends Task {
    public FloatingTask(String title, String description, boolean isComplete)
    {
        super(title, description, isComplete);
    }
}
